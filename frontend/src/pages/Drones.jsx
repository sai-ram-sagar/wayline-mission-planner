import ComingSoon from '../components/ComingSoon';

export default function Drones() {
  return (
    <ComingSoon title="Drone fleet" phase="Phase 5">
      The mock fleet and its assignment table will live here: pick a saved wayline, assign it to one
      or more drones, then advance each assignment through its status.
    </ComingSoon>
  );
}
